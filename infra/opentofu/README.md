# OpenTofu Placeholder

Future public-delivery work will provision:

- a single Hetzner VPS
- firewall rules
- SSH access
- deployment outputs for manual DNS setup

This scaffold intentionally stops short of provisioning because the local MVP and the application surface come first.
